______________________________________________________________________

## name: installer-tester description: Validate install.sh whiptail wizard, text UI fallback, and cron-safe --update (Copilot mirror of .agents skill).

# Installer Tester (Copilot)

Follow the full playbook:

[`.agents/skills/installer-tester/SKILL.md`](../../../.agents/skills/installer-tester/SKILL.md)

Quick validation:

```bash
./tests/run_suite.sh --installer-only
```
